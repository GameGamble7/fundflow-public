# Local-only state for publish.ps1 (the auto-push wrapper).
# Logs grow forever and are not part of the published site.
logs/

# PowerShell artefacts
publish.ps1.local.*
*.bak

# Local deploy script (not part of the published site)
publish.ps1

